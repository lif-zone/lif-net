derry:
- how to sign a frame (with or without the header)

tasks:
- http server
- https server (need ssl certificate)
- dns server
- deployment
  - get real vps
  - create mvp install script
  - install mvp
- lbuffer/scroll - support bytearray
- webserver
  - add map files
  - check bubble configuration (we just need jsx + import handling for browser)
  - bundle js on demand
  - auto-reload on file change
  - generate bundle files in build dir
- rename NodeId -> Id
  - Fix BITS to be 8*sodium.crypto_sign_PUBLICKEYBYTES
- verify we use native webcrpto in crypto-browserify and that we use
  node native crypto in node
- buf_util.js - move to util and rename str to hex (and b2s friends)
